# Pre-Code Decision Sequence — Completed

The previous pre-code sequence has been completed as far as possible outside
the real repository.

## Completed before handoff

- product intent;
- dual-mode architecture;
- 2D/fake-2.5D rendering model;
- canonical world model;
- coordinate architecture;
- V0 projection selection and numeric validation;
- V0 fixture selection/bounds/query;
- data-source/service policy;
- OSM normalization profile;
- CompiledChunk V0 semantic contract;
- road width/surface policy;
- fake-building-depth policy;
- vehicle controller seed parameters;
- V0 deterministic style fallback;
- testing/benchmark protocol;
- V0 technology choices;
- Codex model routing;
- autonomous Codex execution queue.

## Remaining work

Everything remaining requires execution in the actual repository.

Start:

`docs/handoff/CODEX-START-HERE.md`
