# SOLID-05: Long-drive regression

Data: 2026-09-11
Stato: completato
Commit di partenza: `d397b0b`
Dipendenze verificate: SOLID-04 (renderer incrementale attivo nella sessione).

## Risultato

Nuovo test di sessione "drives a long looped route across 100+ windows with
bounded resources": 20 cicli avanti/indietro (2400+1800 passi ciascuno, piu'
di 100 generazioni di domanda) con asserzioni A OGNI passo su record <= 12,
warm cache <= 9, pending <= 32, stato mai "error"; alla fine collider
bounded, dispose pulito (nessun chunk residuo, zero collider). Esegue in
~2 s a clock simulato e sostituisce la prova di 100 finestre statica con il
percorso completo: nessun crash, nessuna unhandled rejection, nessuno stale
apply (guardie esistenti), risorse sempre bounded.

## TDD

- Il test era RED nella forma originale per un limite di record troppo
  stretto (9) durante le inversioni rapide (la finestra transitoria tiene
  fino a 12 chunk attivi, come osservato anche nelle misure E2E): il limite
  e' stato corretto a 12 con la motivazione nel test; nessuna modifica al
  motore e' stata necessaria.
- GREEN stabile; esecuzione ~2 s.

## Modifiche

- `src/app/runtime-session.test.ts`: il nuovo test long-drive.

## Accettazione

- [x] AC1: 100+ generazioni senza crash/unhandled rejection/stale apply
  (generation > 100 asserito; guardie lifecycle invariate).
- [x] AC2: record <= 12, cache <= 9, collider <= 20, pending <= 32 a ogni
  passo.
- [x] AC3: nessun ingresso in celle non applicate (guardia attiva a ogni
  step, invariata dai test ONLINE-11); dispose rilascia tutto.

## Verifiche

| Comando/check | Esito | Note |
| --- | --- | --- |
| Test focalizzati | PASS | runtime-session: 8 test, ~2 s il long-drive |
| typecheck | PASS | `tsc --noEmit` |
| suite completa | PASS | 236 test |
| build | PASS | warning dimensione bundle preesistente |
| E2E / browser | N/A | solo test unitari aggiunti; E2E gia' verde in SOLID-04 |
| lint | N/A | nessuno script lint nel progetto |
| diff/check/status | PASS | `git diff --check` pulito |

## Handoff

Il percorso diventa il letto di regressione per ZOOM-04 (zoom durante la
guida) e per il gate CITY-02. Varianti diagonali e inversioni rapide sono
coperte dai test esistenti (reversal con risposta tardiva, angoli fisici).

## Commit

Commit atomico con il file di test.
