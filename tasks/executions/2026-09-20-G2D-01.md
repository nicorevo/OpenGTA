# G2D-01: Proporzioni taxi, strada e camera — log esecuzione

Data: 2026-09-20. Baseline: `723deb7` (post G2D-00). Scheda: [G2D-01](../gta-2d/G2D-01.md).

## File

- `src/app/camera.ts`: `ZOOM_STEPS` [0.7, 0.85, 6.0, 12.0, 24.0] — il
  livello 2 diventa il preset di guida GTA-2D (8 px/m a 640x480), i
  livelli 3-4 sono target separati di close-view, 0-1 restano far.
- `src/app/camera.test.ts`: aspettative aggiornate (fattori espliciti,
  preset separato dal massimo, >= 30 m di strada davanti all'auto) + 1
  test nuovo (14 test).
- `src/render/pixi/renderer.ts`: `VEHICLE_VISUAL_SCALE` 3.0 -> 1.2
  (intervallo spec 1.0-1.3); fix `changeZoom`: `zoomLevel` aggiornato
  PRIMA del rebuild del tier, altrimenti il culling a far misura con la
  scala del livello precedente (difetto preesistente esposto dal nuovo
  fattore 6, catturato dalla guard `renderer-streaming`).
- `tests/e2e/gta-city.spec.ts`: AC1 (taxi 35-55 x 16-27 px a 640x480,
  strada 6 m >= 2 larghezze auto), AC2 (>= 25 m di strada davanti), scala
  attesa 480/800/390 / 360 x 6, lunghezza visiva 3.5-6 m.
- `tests/e2e/zoom.spec.ts`: AC2 in app reale — zoom +/- via tastiera non
  sposta la posa fisica del veicolo a riposo.
- `tests/e2e/mvt-live.spec.ts` (sesto file, oltre l'ambito dei 5
  dichiarati): AC3 impone che le guard esistenti passino. La guard
  misurava la copertura dello streaming con la vecchia camera default
  (~±320 m); con il nuovo preset (~±50 m) il finestrino P2 copre meno
  celle e i 600 m attraversano 8 chunk invece di 10+. Adeguata guidando
  alla vista far (stessa finestra di prima della ricalibrazione) cosi'
  che la guard misuri lo streaming, non il preset. Nessun altro file
  toccato; deviazione documentata qui.
- `docs/results/GTA-2D-BASELINE.md`: sezione confronto prima/dopo.

## RED

- `camera.test.ts`: 2 falliti (fattori 12/24 attesi, ricevuti 4/14).
- `gta-city.spec.ts`: asserzioni AC1/scala fallite con i fattori vecchi.
- `zoom.spec.ts`: primo tentativo fallito per TDZ (helper `position`
  dichiarato dopo l'uso) — corretto spostando la dichiarazione in alto.

## GREEN e iterazioni

1. `camera.ts` + `renderer.ts` aggiornati -> `camera.test.ts` 14/14,
   `gta-city.spec.ts` + `zoom.spec.ts` verdi.
2. Suite E2E completa (esclusa canary): 1 fallito —
   `renderer-streaming` LOD far atteso 1 cull, ricevuto 0. Causa: rebuild
   del tier con `zoomLevel` ancora al valore precedente (difetto
   preesistente mascherato dai vecchi fattori). Fix in `changeZoom`;
   guard di nuovo verde.
3. Suite E2E completa (esclusa canary): 27/27. Suite unit: 439/439.
   Typecheck, build, `git diff --check` verdi.

## Verifica visiva (test-results, fuori repo)

Analisi pixel dello screenshot 640x480 post-ricalibrazione: taxi arancio
con bbox 34 x 16 px al centro (bounds misurati 37.1 x 17.3),
Viale Centrale 48 px di larghezza con mezzeria bianca che la divide in
due corsie da ~23 px. Inquadratura ~±40 x ±30 m: l'incrocio X riempie la
vista. Numeri registrati in `docs/results/GTA-2D-BASELINE.md`.

## Comandi

```bash
npx vitest run src/app/camera.test.ts
npm run test:run -- --maxWorkers=2
npx playwright test tests/e2e/gta-city.spec.ts tests/e2e/zoom.spec.ts --workers=1
npx playwright test --workers=2 --grep-invert "canary"
npm run typecheck
npm run build
git diff --check
```

## Limiti

- Fattori e scala visiva restano parametri sperimentali (1.2 / 6.0):
  da confermare su GPU/dispositivo reale con il confronto visivo di
  G2D-18.
- La guard mvt-live ora guida alla vista far: la copertura dello
  streaming al preset di guida e' coperta dai 27 E2E ma non dal conteggio
  dei 10 chunk di quella guard.
