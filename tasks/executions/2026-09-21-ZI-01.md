# ZI-01..03: Zoom intermedio (overview → guida) — log esecuzione

Data: 2026-09-21. Baseline: working tree post `df5be7b` (G2D-01). Piano:
sezione "Zoom intermedio (overview → guida)" in [tasks/plan.md](../plan.md).

Richiesta: "uno zoom intermedio tra questi due" (screenshot MVT live in
vista overview). Il salto tra overview (×0.85, livello 1) e preset di guida
(×6.0, livello 2) era 7x, il più grosso della scala: un clic `+` saltava da
quartiere a strada senza vista intermedia.

## File

- `src/app/camera.ts`: `ZoomLevel = 0 | 1 | 2 | 3 | 4 | 5`;
  `ZOOM_STEPS = [0.7, 0.85, 2.25, 6.0, 12.0, 24.0]`;
  `DEFAULT_ZOOM_LEVEL = 3` (exportato, unico fonte del default del renderer);
  `MEDIUM_MAX_ZOOM_LEVEL = 3` (tier: 0-1 far, 2-3 medium, 4-5 near).
  Livello 2 = ×2.25, mediana geometrica di 0.85 e 6.0 (~2.258).
- `src/app/camera.test.ts`: scala a 6 livelli, clamp 0..5, default 3,
  mapping tier aggiornato, nuovo test "ogni passo < 3x".
- `src/render/lod-profile.test.ts`: guard allineate (2-3 medium, 4-5 near,
  livello 2 = medium).
- `src/app/runtime-session.test.ts`: mock tipizzati con `ZoomLevel` importato
  (niente più union inline), clamp 5, scala mock estesa a 6 entry.
- `src/render/pixi/renderer.ts`: zoom iniziale e tier iniziale da
  `DEFAULT_ZOOM_LEVEL` (niente littérale 2).
- `src/app/bootstrap.ts`: placeholder livello 3, clamp superiore 5.
- `tests/e2e/zoom.spec.ts`: default 3, clamp 5, cinque `-` per tornare a 0.
- `tests/e2e/renderer-streaming.spec.ts`: default 3, inLevels `[4, 5, 5]`,
  outLevels `[4, 3, 2, 1, 0, 0, 0, 0]`.
- `tests/e2e/gta-city.spec.ts` + `tests/fixtures/gta-city.ts`: default
  livello 3 (fattore 6.0 invariato → misure pixel AC1/AC2 invariate).
- `tests/bench/zoom-demand.test.ts`: report esteso a 6 livelli.
- `README.md`: zoom 6 livelli con intermedio ×2.25.
- Docs: `docs/results/INTERMEDIATE-ZOOM-RESULT.md`, piano/todo, CURRENT.md.

## RED

- `camera.test.ts`: 9/15 falliti con la scala vecchia (fattori [.., 6, 12, 24]
  vs attesi con 2.25, mapping tier 2-3 medium/4-5 near, clamp 5).

## GREEN

- `camera.ts` aggiornato → `camera.test.ts` 15/15 (un errore di battitura
  nell'array atteso del test passi <3x corretto durante il ciclo: primo
  rapporto 0.85/0.7, non 2.25/0.7).
- Suite unit: 440/440 (era 438: +1 test "passi <3x", guard aggiornate).
- E2E mirati (zoom, renderer-streaming, gta-city): 6/6.
- E2E completa: 27 passed + 1 canary skip (skip per design).
- Typecheck, build, bench `zoom-demand` (6 livelli): verdi.

## Comandi

```bash
npm run test:run -- src/app/camera.test.ts
npm run typecheck
npm run test:run
npm run build
npx vitest run --config vitest.bench.config.ts tests/bench/zoom-demand.test.ts
npm run test:e2e -- tests/e2e/zoom.spec.ts tests/e2e/renderer-streaming.spec.ts tests/e2e/gta-city.spec.ts --workers=1
npm run test:e2e
git diff --check
```

## Limiti

- Fattore 2.25 sperimentale: ricalibrabile solo in `ZOOM_STEPS` (guard
  "passi <3x" + confronto visivo G2D-16/18).
- Verifica visiva sul live MVT da confermare a schermo: il nuovo livello
  mostra ~5-6 isolati su ~950 px di altezza (tra l'overview del
  screenshot e la strada del preset di guida).
- I doc storici (spec close-zoom/city-drive-stable, risultati passati)
  restano invariati per convenzione: descrivono lo stato alla consegna.
