# RV-11: Rimozione dead code — log esecuzione

Data: 2026-09-17.

## Cosa è cambiato (rimozioni)
- `src/render/pixi/first-person/sky-drawer.ts`: file rimosso (34 righe).
  Non importato da nessun file `src/` né `tests/` (`first-person-renderer.ts`
  ha il proprio `drawSky` locale, ancora in uso).
- `src/render/pixi/first-person/camera3d.ts`: rimossa `projectPerspective`
  e l'interfaccia `ProjectedPoint` che serviva solo a lei (53 righe).
  `projectNdc`/`projectGroundNdc` restano (usati da
  `building-projector.ts`/`road-projector.ts`).
- `src/render/pixi/first-person/camera3d.test.ts`: rimossi i 10 test di
  `projectPerspective` (62 righe); resta il test sui valori di
  `DEFAULT_CAMERA_CONFIG`.
- `src/app/runtime-session.ts`: rimossa la riga
  `const next = new Map(chunks); next.set(id, { key, chunk });` — `next`
  non era letto da nessuna parte; l'aggiornamento di stato effettivo
  (`chunks.set(id, …)` dopo il try/catch) e il rollback su `previous` sono
  invariati.

## Deviazione dalla scheda (item 4): `retry.test.ts` conservato
La scheda indicava di rimuovere `src/world/runtime/retry.test.ts` perché
"testa un modulo `retry.ts` che non esiste più". Verifica sul codice
attuale: il file **non importa `retry.ts`** (che infatti non esiste) ma
`createOverpassGeoDataSource` da `./source.ts`, e testa live il comportamento
di retry del provider Overpass. È l'**unica** copertura di:
- parse di `Retry-After` (numeri, valori invalidi, HTTP-date) — funzione
  `retryAfterMilliseconds` (`source.ts:157`), senza altri test;
- timing dei retry (backoff 2 s/5 s) e spacing tra job;
- cooldown provider che persiste oltre il budget attivo sul job successivo;
- abort durante il backoff senza ulteriore tentativo;
- classificazione status transienti (400/403 non ritentati; 429/500/503 con
  al più 3 tentativi).

Né `request-scheduler.test.ts` (nessun test su `deferUntil`/429) né
`source.test.ts` (429 solo nei path fallback/no-retry con opzioni esplicite)
coprono questi comportamenti: la rimozione avrebbe significato perdita
reale di copertura, in contrasto con la stessa verifica richiesta dalla
scheda ("verificare l'assenza di perdita di copertura"). Il test file resta,
verificato 15/15 verde.

## TDD
N/A (rimozioni). Verifiche AC:
- AC1: zero riferimenti per ogni simbolo rimosso — verifica con `rg`-equivalent
  (grep) su `src/`+`tests/`: nessun import di `sky-drawer`, nessuna chiamata a
  `projectPerspective`/`ProjectedPoint` fuori dal test rimosso, `next` senza
  letture; la suite completa passa dopo le rimozioni.
- AC2: suite completa + typecheck verdi (vedi evidenze).
- AC3: `git diff` contiene solo le rimozioni e i test associati
  (149 righe rimosse, 1 inserita: la riga import del test ridimensionata).

## Evidenze gate
- `npm run typecheck`: verde.
- `npm run test:run`: 402/402 (412 pre-RV-11 − 10 test `projectPerspective`
  rimossi; `retry.test.ts` 15/15 conservato e verde).
- `npm run build`: verde (solo warning chunk size preesistente).
- `npm run test:e2e`: 23 passed + 1 skipped (preesistente) — include il boot
  dell'app (percorso toccato dalla pulizia di `runtime-session.ts`).
