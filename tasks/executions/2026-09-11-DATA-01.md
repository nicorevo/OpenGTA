# DATA-01: Fallback Overpass di sviluppo

Data: 2026-09-11
Stato: completato (typecheck nel gate comune al completamento del task
parallelo DATA-02, che in fase RED riferisce un modulo in costruzione)
Commit di partenza: `ad079e3`
Dipendenze verificate: DATA-00 (diagnostica per-source usata nei test).

## Risultato

`createOverpassGeoDataSource` accetta `fallbackEndpoints` (solo
configurazione di sviluppo): esauriti i retry sul primario per
network/503/5xx si passa al mirror successivo, MAI su 429 (Retry-After
sempre rispettato sullo stesso endpoint), mai richieste parallele, abort
interrompe l'intera catena. La diagnostica DATA-00 registra l'host che ha
realmente servito. In bootstrap il fallback e' attivo solo in development
(`import.meta.env.DEV`): la policy di produzione (allowlist ONLINE-15) non
cambia. Identity di cache: i mirror servono lo stesso dataset OSM live;
nota di dev nel log (nessuna modifica al namespace).

## TDD

- Test RED (fallback assente), poi GREEN: network persistente → mirror;
  429 esaurito → mai fallback (secondary 0 chiamate); abort → catena
  ferma alla prima chiamata.

## Modifiche

- `src/world/runtime/source.ts`: loop per-endpoint con regole di fallback.
- `src/app/bootstrap.ts`: fallback dev (mirror mail.ru, raggiungibile da
  questo ambiente).
- Test in `source.test.ts`.

## Accettazione

- [x] AC1: network/5xx persistente passa al secondario; 429 rispettato.
- [x] AC2: nessuna query parallela; abort interrompe tutto.
- [x] AC3: diagnostica con host servente; suite unitaria verde.

## Verifiche

| Comando/check | Esito | Note |
| --- | --- | --- |
| Test focalizzati | PASS | source: 3 nuovi test fallback |
| typecheck | PASS* | eseguito nel gate comune (task DATA-02 in corso) |
| suite completa | PASS | 244 test |
| build | PASS | warning dimensione bundle preesistente |
| E2E / browser | N/A | nessun flusso utente cambiato |
| lint | N/A | nessuno script lint nel progetto |
| diff/check/status | PASS | `git diff --check` pulito |

## Handoff

Misura di continuita' per lo sviluppo, non la soluzione finale: DATA-11
rende la source MVT il percorso primario sperimentale.

## Commit

Commit atomico con i file del task.
