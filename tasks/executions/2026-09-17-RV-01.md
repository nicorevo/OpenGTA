# RV-01: Cancel stream MVT sul budget — log esecuzione

Data: 2026-09-17. Baseline: `ae92e10`.

## Cosa è cambiato
`src/world/runtime/vector-tile/provider.ts` — `readBoundedBytes`: flag
`complete` impostato solo al ritorno del valore; nel `finally` il reader viene
cancellato (`reader.cancel()`) quando la lettura non è completa (errore o
abort), come nel
pattern di riferimento `src/world/runtime/response-reader.ts:35-36`. Prima del
fix, un tile oltre il budget lasciava lo stream a scaricare il resto del
payload.

## TDD
- RED: test "cancels the stream when the byte budget is exceeded"
  (`provider.test.ts`): stream mockato che traccia `cancel()` e continua a
  produrre oltre il budget; falliva con `expected false to be true`.
- GREEN: fix applicato; test verde.

## Evidenze gate
- `npm run typecheck`: verde.
- `npm run test:run`: 356/356 (era 355; +1 test).
- `npm run build`: verde.
