# RV-02: Re-sync SECURITY.md con il codice — log esecuzione

Data: 2026-09-17.

## Cosa è cambiato (solo SECURITY.md)
1. "Stato attuale": la persistenza client non è più tra le superfici
   inesistenti; indicata la cache IndexedDB dei chunk compilati (best-effort,
   bounded).
2. Tabella presidi:
   - "Richieste al provider": aggiunto il fallback secondario **solo-dev** per
     Overpass (`bootstrap.ts:102`, assente dal build di produzione);
   - nuova riga "Richieste tile MVT" (`vector-tile/provider.ts`): chiave tile
     validata, endpoint fisso, dataset pinnato, timeout 30 s, budget 16 MiB con
     `cancel()` a overrun, 404/204 vuota deterministica, errori distinti;
   - "Payload OSM e ReadableStream": budget distinti (JSON 8 MiB / tile MVT
     16 MiB);
   - nuova riga "Cache persistente" (`persistent-*.ts`, `bootstrap.ts:61`):
     best-effort, budget 32 MiB con eviction LRU, chiave versionata, validazione
     alla lettura con scarto come miss;
   - "Diagnostica in pagina": documentato `chunks()` (chunk attivi compilati,
     dati pubblici OSM).
3. "Browser, cache e storage": lo storage persistente è descritto al presente
   (quota, eviction, invalidazione, best-effort) invece di "quando verrà
   introdotto".
4. "Superfici future": rimossa la voce "Storage persistente" (ora esiste).

## Gate di consistenza documentale (esito)
- Passo 2 (frasi obsolete): 1 hit in `tasks/city/SOLID-06.md` — legittimo
  (scheda di task che cita la frase obsoleta come oggetto storico del lavoro).
- Passo 3 (superfici vs codice): `indexedDB` = 2 file in `src/` — ora
  dichiarata nella tabella dei presidi (era l'unico hit); Worker/localStorage/
  CSP/sendBeacon/telemetry = 0.
- Passo 5 (claim numerici): 8 MiB = `DEFAULT_RESPONSE_BYTES`, 16 MiB =
  `bootstrap.ts:108`, 32 MiB = `DEFAULT_PERSISTENT_BUDGET_BYTES`, capacity 9 =
  `createChunkCache(9)` — tutti ricontrollati nel codice.
- File citati nei presidi: tutti esistenti.

Nota di processo: il retry MVT che onora `Retry-After` è promesso da
"Richieste al provider" per il percorso Overpass; sul percorso MVT il retry
viene introdotto da RV-04 e la riga sarà aggiornata in quel commit, così ogni
commit resta coerente documenti/codice.

## Verifica
`npm run typecheck` verde; `npm run test:run` 365/365 (nessuna modifica di
codice).
