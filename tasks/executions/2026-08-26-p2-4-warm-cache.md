# Execution Log: P2.4 Warm Cache

**Data:** 2026-08-26
**Obiettivo:** aggiungere una cache compilata in-memory con eviction bounded.

## Implementazione

- Aggiunto `src/world/chunk/cache.ts` come cache generica priva di dipendenze
  runtime.
- L’identità della voce include sempre `chunkId` e `compilerVersion`.
- Le letture aggiornano la recency; il superamento della capacità espelle la
  voce meno recentemente usata.
- Aggiunti `evict` e `clear` per rimozioni esplicite.

## Verifiche

- `npm run test:run -- src/world/chunk/cache.test.ts`: PASS — 4 test.
- `npm run typecheck`: PASS.
- `npm run test:run`: PASS — 18 file, 68 test.
- `npm run build`: PASS; warning noto sul chunk PixiJS principale.

## Scope escluso

La cache è solo in-memory: non include IndexedDB, serializzazione, asset
renderer/physics o acquisizione geografica; seguiranno le slice dedicate.
