# Veicolo: velocità -20% e divieto d'ingresso in acqua (WS-01..02)

Data: 2026-09-21. Persona: fullstack-developer.
Richiesta utente: "diminuisci un pochino la velocità (20%) e non consentire
l'ingresso in acqua".
Baseline di partenza: `04d2b64` (working tree pulito, 472 unit verdi).

## WS-01 — `VEHICLE_TUNING` -20% (TDD)

- RED: `src/gameplay/vehicle/controller.test.ts` aggiornato (top speed
  84 → 67.2 nel test di accelerazione e nel cap, nuovo test del cap in
  marcia inversa a 11.2) → 3 falliti.
- GREEN: `src/gameplay/vehicle/controller.ts` — `maxForwardSpeed` 84 → 67.2
  (~242 km/h), `maxReverseSpeed` 14 → 11.2. Accelerazioni, freno, grip e
  steering invariati (solo il soffitto di velocità, come richiesto).
- 9/9 in `controller.test.ts`.

## WS-02 — divieto d'ingresso in acqua (TDD)

- RED: `src/world/compiler/compiled.test.ts` — nuovo test "compiles water
  areas into collision shapes…" (lago → collision poligonale; fiume
  solo-a-linea → nessuna collisione) → 1 fallito.
- GREEN: `compileRegion` (`src/world/compiler/compiled.ts`) emette
  `collisions.push({ kind: "polygon", featureId, polygon: water.area })`
  per ogni water area, accanto alla visual ground. L'adapter Rapier trasforma
  già i poligoni in muri perimetrali (`buildingWall`, anelli outer+holes):
  il veicolo non può attraversare il perimetro, come per gli edifici.
- Bump cache persistente: `compilerVersion` in
  `src/app/runtime-session.ts` `"v0-runtime"` → `"v0-runtime-water-collision"`.
  Senza il bump i chunk già in IndexedDB (compilati senza i muri d'acqua)
  verrebbero riusati in silenzio (la chiave include la versione del
  compiler; record di versione diversa = miss garantito).
- 13/13 in `compiled.test.ts`.

## Composizione del comportamento

- "Non entrare in acqua" = il compilatore emette il muro perimetrale (test
  WS-02) + l'adapter blocca l'attraversamento dei muri poligonali (test
  esistenti "keeps a dynamic vehicle outside a static wall" e il rollback
  post-step "restores the previous valid pose…").
- La spawn già evita l'acqua (`src/gameplay/vehicle/spawn.ts` filtra le water
  areas dalle pose ammesse) → il veicolo non nasce mai intrappolato.

## Gate completa

- `npm run test:run`: **474/474** (472 + 2 nuovi: cap inversa, water
  collisions).
- `npm run typecheck`: pulito.
- `npm run build`: verde (solo il warning di chunk size preesistente).
- `npm run test:e2e`: **34 passed + 1 canary skipped** (nessuna regressione
  nei test che fanno guidare il veicolo: origine di Lecce priva di water
  areas raggiungibili, e `live-startup`/`zoom`/`touch-controls` verdi).
- `git diff --check`: pulito.

## Note

- La verifica visiva finale ("gioco e non entro in mare") è lasciata all'utente:
  con il default MVT a Lecce, il perimetro del lungomare/Adriatico è ora
  un muro perimetrale; stesso comportamento per laghi/parchi-acqua OSM.
- Se un giorno servirà nuotare/galleggiare, il punto di estensione è
  già isolato: rimuovere la collisione dal compilatore e aggiungere una
  dinamica di galleggiamento nel controller.
