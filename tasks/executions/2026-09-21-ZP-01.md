# Luogo corrente nella barra di stato (ZP-01..04)

Data: 2026-09-21. Persona: fullstack-developer.
Spec: `docs/specs/current-place-name-v1.md` (approvata dall'utente con "procedi"
dopo la valutazione delle opzioni A/B/C).
Baseline: `04d2b64` + working tree con la tranche WS (velocità/acqua) ancora da
commitare — i commit WS e ZP restano separati.

## ZP-01 — `geocode.ts` `reverse()` (unit, TDD)

- RED: 7 test reverse aggiunti a `src/app/geocode.test.ts` (endpoint pinnato,
  serializzazione lat/lon + format/zoom/accept-language, mapping con
  coordinate richieste, `{error}` → `undefined`, non-oggetto/array/
  display_name assente → `invalid-response`, 429 → `rate-limited` + timeout,
  abort del caller → `aborted`). Prima esecuzione: 7 failed / 17 passed.
  Fix di test: import vitest esteso a `describe` (blocco reverse raggruppato).
- GREEN: `NOMINATIM_REVERSE_URL` + opzione `reverseEndpoint`; il plumbing di
  fetch (AbortController + timer + forwarding abort + `readBoundedJson` +
  mapping `GeocodeError`) estratto in un helper interno `fetchJson` condiviso
  da `search` e `reverse`; `reverse(lat, lon, signal?)` valida i range
  lat ±90 / lon ±180, accetta solo un singolo oggetto, `{error}` → `undefined`,
  `display_name` 1..256 trimmed, `place_id` → `placeId` (stringa o `""`),
  lat/lon del candidato = valori richiesti, nessuna cache.
- 24/24 in `src/app/geocode.test.ts` (i 17 preesistenti di `search` passano
  intatti → refactor comportamento-invariato).

## ZP-02 — `src/app/place-status.ts` (unit, TDD)

- RED: `src/app/place-status.test.ts` (10 test): `zoneKeyForPose` (stabilità,
  confini di cella inclusi i negativi, cell size custom) + tracker (1 richiesta
  per zona, nessuna per stessa zona, zona nuova dopo la precedente senza mai 2
  in-flight, intervallo minimo con retry della zona pendente, in-flight singolo
  con zona queued, fallimento mantiene il nome e ritenta, "nessun dato" marca
  la zona servita, `dispose` aborta + no-op).
  Fix di test: (1) punto di stabilità `(0.5, -0.5)` → `-0.5` è nella cella
  -1 (corretto del test, il modulo aveva ragione); (2) l'atteso "abort della
  precedente al cambio zona" è incompatibile col design finale (le richieste
  sono sequenziali: la seconda parte solo alla risoluzione della prima; l'abort
  esiste solo su `dispose`) → test riscritto e spec allineata.
- GREEN: `PLACE_ZONE_METERS`, `zoneKeyForPose`, `createPlaceTracker` con
  `servedZone` / `activeKey` / `pendingZone` / `lastRequestAt` / `placeName`;
  `track` è l'unico punto di fire, `tryPending` rilancia la zona pendente alla
  risoluzione se l'intervallo è decorso; il fallimento NON marca la zona
  servita (retry), il "nessun dato" la marca (zero retry).
- 10/10 in `src/app/place-status.test.ts`.

## ZP-03 — wiring bootstrap + e2e (TDD)

- RED: `tests/e2e/place-status.spec.ts` (4 test). Scelta chiave per il RED→
  GREEN: lo stato `ready` richiede zero errori di runtime e gli errori sono
  per-tile → i tile della finestra iniziale vanno serviti davvero: tutti gli
  URL MVT rispondono con i byte del fixture `lecce-z14-openfreemap.pbf` (lo
  stesso trick di `live-config`/`mvt-live`), reverse mockato con `page.route`.
  RED confermato 3/4 (il test offline era già verde di suo — guard di
  regressione).
- GREEN: `bootstrap.ts` — tracker creato solo in open-world con sessione;
  proiettore `createTangentProjector(config.origin)` una volta (l'origine è
  la stessa della sessione, quindi `unproject` è coerente); `updateStatus()`
  (200 ms) fa `track(pose)` e per `ready` mostra `place() ?? "Area pronta"`;
  `placeTracker.dispose()` in `stopLoop` (stop e restart).
- Fix di test: toleranza lat/lon dell'assert "near origin" 0.001° → 0.01°
  (lo spawn è su una strada a ~150 m dall'origine, ancora nella prima zona
  1000 m; il comportamento di zona è coperto dai unit).
- 4/4 in `tests/e2e/place-status.spec.ts`.
- Conseguenza trasversale: 12 test e2e preesistenti (8 file) fallivano perché
  la loro network guard "nessuna chiamata esterna inattesa" intercettava il
  nuovo reverse. Fix: `tests/fixtures/geocode-mock.ts`
  (`mockReverseGeocoding(page)`, registrato DOPO la guard — in Playwright la
  route registrata per ultima ha la precedenza) con payload `{error}`
  deterministic ("nessun dato" → messaggio di stato invariato); integrato in
  live-config (×2), live-startup (×2), incremental-renderer, measurements,
  persistent-cache (solo il test che avvia una sessione live), live-streaming,
  mvt-live, zoom. gta-city e bootstrap.spec non toccati (non raggiungono
  `ready`).

## ZP-04 — Gate completa

- `npm run test:run`: **491/491** (59 file). Un flake temporaneo in
  `runtime-session.test.ts` (test di timing, 5.7 s) è passato al rerun singolo
  e alla suite completa — non correlato ai file toccati da ZP.
- `npm run typecheck`: pulito.
- `npm run build`: verde.
- `npm run test:e2e`: **38 passed + 1 canary skipped, 0 failed** (run completo
  dopo l'integrazione di `mockReverseGeocoding`; prima: 12 failed).
- `git diff --check`: pulito.
- Docs: `SECURITY.md` (riga geocoding estesa al reverse), result
  `docs/results/CURRENT-PLACE-NAME-RESULT.md`, questo log, `tasks/plan.md` +
  `tasks/todo.md` (ZP-01..04 spuntati), `docs/handoff/CURRENT.md`.
