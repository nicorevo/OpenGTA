# RV-03: Test bench nel gate standard — log esecuzione

Data: 2026-09-17.

## Cosa è cambiato
- `vitest.config.ts`: include `tests/bench/**/*.test.ts` nel gate standard,
  escluso `mvt-benchmark.test.ts` (fetch di rete reale, non deterministico:
  resta eseguibile solo via `npm run test:bench` con `vitest.bench.config.ts`).
- `tests/bench/mvt-parity.test.ts`: la scrittura del report in
  `docs/analysis/MVT-LECCE-PARITY.md` è condizionale su
  `OPENGTA_WRITE_BENCH_REPORT=1`; le asserzioni sulla matrice di parità restano
  incondizionate (è il gate di parità del cambio provider).
- `package.json`: nuovo script `test:bench`.

## Evidenze
- Prima: 48 file / 356 test nel gate standard; i bench erano esclusi.
- Dopo: 51 file / 365 test (+9: pathological-rings 6, mvt-parity 2,
  zoom-demand 1), tutti verdi in ~3 s.
- `git status --short` pulito dopo l'esecuzione del gate (nessun side effect
  su `docs/analysis/`). Nota: un run precedente del bench config, fatto prima
  della conditional, aveva aggiornato il report (hash commit cambiato); il
  file è stato ripristinato e il gate standard non lo tocca più.

## Gate
- `npm run typecheck`: verde.
- `npm run test:run`: 365/365.
- `npm run build`: verde.
