# RV-12: Batch nits

**Stato:** non avviato. **Dipendenze:** RV-11. **Persona:** fullstack-developer. **Taglia:** S.

## Obiettivo
1. `src/app/metrics.ts:37` — `frameTimes.shift()` O(n) a ogni overflow del
   buffer (1 800 entry → a ogni frame a 60 fps): ring buffer.
2. `src/world/runtime/vector-tile/provider.ts:71` — `Retry-After` parsato solo
   come delta-seconds numerico: aggiungere il formato HTTP-date, allineato a
   `source.ts` che gestisce entrambi.
3. `fragmentRoad` in `src/world/compiler/partition.ts` — tutti i frammenti di
   centerline della stessa way condividono un unico poligono superficie: una
   strada a U dentro una cella genera un "ponte" sulla superficie: spezzare la
   superficie per frammento.
4. `src/gameplay/vehicle/shape.ts:10` — riga ~140 caratteri: espandere.

## TDD
1. RED:
   - metrics: invariant del ring buffer (capacità costante, ordine dei campioni,
     niente O(n) allo shift);
   - Retry-After: data HTTP futura → delay corretto (fake timers);
   - strada a U (fixture sintetica): la superficie non contiene geometria
     nell'incavo della U (confronto con riferimento per-frammento).
2. Implementare; GREEN.

## Accettazione
- [ ] AC1: metrics senza O(n) nel loop di frame.
- [ ] AC2: entrambi i formati di Retry-After gestiti dal provider MVT.
- [ ] AC3: nessun "ponte" di superficie sulle strade a U.
- [ ] AC4: gate completo verde.

## Verifica
`npm run test:run` + gate comune.
