# RV-09: Pre-filtro bbox relazioni OSM

**Stato:** completato (2026-09-17, log in `../executions/2026-09-17-RV-09.md`). **Dipendenze:** nessuna. **Persona:** fullstack-developer. **Taglia:** S.

## Obiettivo
`addRelationBuildings` in `src/geo/normalize/osm.ts:347-386`: point-in-ring
O(holes × outers × ring) senza pre-filtro; un multipolygon patologico (membri
cap 20 000) può bloccare il main thread per secondi. Pre-filtro bbox:
calcolare il bbox di ogni outer ring una volta e scartare la coppia
(hole, outer) se i bbox non si intersecano, prima del point-in-ring.
Comportamento identico sui dati validi.

## READ
`osm.ts` (sezione relazioni + guard DoS esistenti), `osm.test.ts`,
`tests/bench/pathological-rings.test.ts`.

## MAY MODIFY / DO NOT TOUCH
Modificabili: `osm.ts` + i suoi test. Non toccare altri normalizzatori.

## TDD
1. RED: multipolygon patologico con outer ring a bbox disgiunti → nessun
   point-in-ring sulle coppie scartate (misurare con spy sul helper puro o
   con budget di tempo), stesso risultato del riferimento.
2. Implementare il pre-filtro; GREEN.

## Accettazione
- [x] AC1: nessun point-in-ring sulle coppie rifiutate dal bbox.
- [x] AC2: fixture Lecce + `osm.test` + `lecce.integration.test` verdi
      (output identico).
- [x] AC3: `pathological-rings` (gate RV-03) verde.

## Verifica
`npm run test:run -- src/geo` + gate comune.
