# RV-02: Re-sync SECURITY.md con il codice

**Stato:** completato. **Dipendenze:** nessuna. **Persona:** software-architect. **Taglia:** S.

## Obiettivo
Allineare `SECURITY.md` al codice attuale (il codice è fonte di verità,
verificato). Drift identificati dalla review:
1. "Non esistono ancora nel codice: persistenza client (IndexedDB...)"
   (righe 17-21) → esiste la cache IndexedDB dei chunk compilati
   (`src/world/chunk/persistent-*.ts`, wired in `src/app/bootstrap.ts`),
   budget 32 MiB, best-effort, con validazione alla lettura.
2. "endpoint fisso configurato (nessuna rotazione di mirror)" (riga 33) →
   descrivere il fallback mirror **solo-DEV** (`src/app/bootstrap.ts`), assente
   dal build di produzione.
3. "budget di byte (8 MiB di default)" (riga 34) → il budget MVT tile è
   16 MiB (`bootstrap.ts`), Overpass 8 MiB: distinguere i due.
4. "Warm cache ... nessuna persistenza tra sessioni" (riga 37) → aggiungere
   l'IndexedDB store (capacità, chiave, budget, evizione).
5. `__opengtaV0Debug` "solo stato di gioco, contatori e tempi" (riga 40) →
   descrivere `chunks()` che espone i chunk compilati (dati OSM pubblici,
   handle locale).
Poi eseguire il gate di consistenza documentale e chiudere altre drift emerse.

## READ
`SECURITY.md`, `src/app/bootstrap.ts`, `src/world/chunk/persistent-indexeddb.ts`,
`docs/process/documentation-consistency-gate.md`.

## MAY MODIFY / DO NOT TOUCH
Modificabile: `SECURITY.md` (e altro documento solo se il gate lo indica).
Non toccare il codice (se il drift fosse solo-documentale, come atteso).

## Verifica
Gate di consistenza documentale + verifica claim-per-claim contro il codice
(file:line). Gate comune (nessuna modifica di codice attesa).

## Accettazione
- [x] AC1: ogni affermazione di stato e della tabella è verificabile nel codice.
- [x] AC2: nessuna affermazione residua in contrasto (es. "IndexedDB" come
      superficie futura inesistente).
