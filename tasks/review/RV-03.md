# RV-03: Test bench nel gate standard

**Stato:** completato. **Dipendenze:** nessuna. **Persona:** fullstack-developer. **Taglia:** S.

## Obiettivo
`tests/bench/*.test.ts` (guard DoS `pathological-rings`, gate di parità
`mvt-parity`, `zoom-demand`, `mvt-benchmark`) sono esclusi da
`npm run test:run` (vitest.config.ts include solo `src/**`): la baseline
355/355 non copre le regressioni più rilevanti del cambio provider. Includerli
nel gate standard e isolare i test con side effect (scrittura di report in
`docs/analysis/`): scrivere solo se un env var è esplicitamente impostata.

## READ
`vitest.config.ts`, `tests/bench/*.ts`, `package.json` (script).

## MAY MODIFY / DO NOT TOUCH
Modificabili: `vitest.config.ts`, script di `package.json`, `tests/bench/*`.
Non toccare `src/`.

## TDD
1. RED: aggiungere `tests/bench/**/*.test.ts` a `include` ed eseguire:
   emergono i test con side effect (report scritto a ogni run).
2. Rendere la scrittura del report condizionale (env var
   `OPENGTA_WRITE_BENCH_REPORT=1`); GREEN.

## Accettazione
- [x] AC1: `npm run test:run` include pathological-rings, mvt-parity, zoom-demand.
- [x] AC2: il gate non produce modifiche in `docs/analysis/` (git status pulito).
- [x] AC3: gate completo verde.

## Verifica
`npm run test:run` + `git status --short` + gate comune.
