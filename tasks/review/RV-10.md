# RV-10: Validazione elementare codec persistente

**Stato:** completato (2026-09-17, log in `../executions/2026-09-17-RV-10.md`). **Dipendenze:** nessuna. **Persona:** fullstack-developer. **Taglia:** S.

## Obiettivo
`src/world/chunk/persistent-codec.ts`: la deser valida la forma (array,
numeri) ma non gli elementi: un record corrotto (null/NaN/Infinity in
coordinate) potrebbe arrivare a renderer/fisica. Validare gli elementi alla
deser: tutti i campi numerici finiti, coordinate entro un sanity bound
(±2000 km), geometrie non vuote e ben formate; record non valido → miss
deterministico (stesso trattamento di una entry corrotta).

## READ
`persistent-codec.ts`, `persistent-indexeddb.ts` (gestione del miss),
`src/world/model/types.ts`.

## MAY MODIFY / DO NOT TOUCH
Modificabili: `persistent-codec.ts` + i suoi test. Non toccare il contratto
`PersistentChunkStore`.

## TDD
1. RED: serialize → corrompi (NaN, elemento null, Infinity) → deserialize →
   miss.
2. Implementare; GREEN.

## Accettazione
- [x] AC1: record corrotti non producono mai un valore (miss deterministico).
- [x] AC2: i round-trip validi restano invariati (test esistenti verdi).

## Verifica
`npm run test:run -- src/world/chunk` + gate comune.
