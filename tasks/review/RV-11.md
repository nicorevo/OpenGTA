# RV-11: Rimozione dead code

**Stato:** non avviato. **Dipendenze:** RV-05..07 (dopo i gate). **Persona:** fullstack-developer. **Taglia:** S.

## Obiettivo
Rimovere il codice morto verificato dalla review (con `find_referencing_symbols`
per ogni simbolo):
1. `src/render/pixi/first-person/sky-drawer.ts` — non importato da nessuno
   (`first-person-renderer.ts` ha il proprio `drawSky`).
2. `projectPerspective` in `src/render/pixi/first-person/camera3d.ts:70` —
   usato solo dai propri test: rimuovere funzione e relativi test.
3. `src/app/runtime-session.ts:51` — `const next = new Map(chunks); next.set(...)`
   mai letto: rimuovere, conservando l'aggiornamento di stato effettivo.
4. `src/world/runtime/retry.test.ts` — testa un modulo `retry.ts` che non
   esiste più (la logica vive in `request-scheduler.ts`, testato separatamente):
   rimuovere il test, verificare l'assenza di perdita di copertura.

## READ
I file citati + verifica riferimenti per ogni simbolo.

## TDD
N/A (rimozioni). Verifica: zero riferimenti, suite verde, diff minimo.

## Accettazione
- [ ] AC1: ogni elemento rimosso ha zero riferimenti (verificato con strumenti
      simbolici e `rg`).
- [ ] AC2: suite completa + typecheck verdi.
- [ ] AC3: `git diff` contiene solo le rimozioni e i test associati.

## Verifica
`rg` sui nomi rimossi + gate comune.
