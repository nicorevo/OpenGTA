# RV-11: Rimozione dead code

**Stato:** completato con deviazione (2026-09-17, log in `../executions/2026-09-17-RV-11.md`: item 4 conservato, `retry.test.ts` è copertura live di `source.ts`, premessa della scheda non più valida). **Dipendenze:** RV-05..07 (dopo i gate). **Persona:** fullstack-developer. **Taglia:** S.

## Obiettivo
Rimovere il codice morto verificato dalla review (con `find_referencing_symbols`
per ogni simbolo):
1. `src/render/pixi/first-person/sky-drawer.ts` — non importato da nessuno
   (`first-person-renderer.ts` ha il proprio `drawSky`).
2. `projectPerspective` in `src/render/pixi/first-person/camera3d.ts:70` —
   usato solo dai propri test: rimuovere funzione e relativi test.
3. `src/app/runtime-session.ts:51` — `const next = new Map(chunks); next.set(...)`
   mai letto: rimuovere, conservando l'aggiornamento di stato effettivo.
4. ~~`src/world/runtime/retry.test.ts`~~ — **conservato**: il file importa
   `./source.ts` (esistente) e testa live il retry del provider Overpass
   (parse `Retry-After`, backoff, cooldown tra job, abort nel backoff,
   status transienti). È l'unica copertura di quei comportamenti; rimuoverlo
   causerebbe perdita di copertura reale (vedi log esecuzione).

## READ
I file citati + verifica riferimenti per ogni simbolo.

## TDD
N/A (rimozioni). Verifica: zero riferimenti, suite verde, diff minimo.

## Accettazione
- [x] AC1: ogni elemento rimosso ha zero riferimenti (verificato con grep su
      `src/`+`tests/`; `rg` non disponibile nel sandbox).
- [x] AC2: suite completa + typecheck verdi (402/402, build e e2e verdi).
- [x] AC3: `git diff` contiene solo le rimozioni e i test associati.

## Verifica
`rg` sui nomi rimossi + gate comune.
