# Routing degli intenti

La mappatura tra intento, agente e skill è consolidata in `AGENTS.md` alla
radice del progetto (auto-caricato da opencode). Quello è l'unica fonte di
verità; consulta lì.

Questo file esiste solo come segnaposto di compatibilità per gli strumenti che
scoprono `.opencode/agents/AGENTS.md`.
